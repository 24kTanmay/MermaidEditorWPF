<Application x:Class="Mermaid.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Mermaid"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- MODERN DARK PALETTE (VS Code / Linear inspired) -->
            <SolidColorBrush x:Key="BgBody" Color="#18181b"/>
            <SolidColorBrush x:Key="BgHeader" Color="#202022"/>
            <SolidColorBrush x:Key="BgTabStrip" Color="#18181b"/>
            <SolidColorBrush x:Key="BgEditor" Color="#1f1f1f"/>
            <SolidColorBrush x:Key="BgGutter" Color="#1f1f1f"/>
            <SolidColorBrush x:Key="BgPreview" Color="#1a1a1a"/>
            <SolidColorBrush x:Key="BgSidebar" Color="#18181b"/>
            <SolidColorBrush x:Key="BgToolbar" Color="#1f1f1f"/>
            <SolidColorBrush x:Key="BgStatus" Color="#8b5cf6"/>
            
            <SolidColorBrush x:Key="BorderColor" Color="#3f3f46"/>
            <SolidColorBrush x:Key="BorderHover" Color="#3E3E3E"/>
            
            <SolidColorBrush x:Key="TextPrimary" Color="#d4d4d8"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#71717a"/>
            <SolidColorBrush x:Key="TextActive" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="TextAccent" Color="#a1a1aa"/>
            
            <SolidColorBrush x:Key="AccentColor" Color="#8b5cf6"/>
            <SolidColorBrush x:Key="AccentHover" Color="#9084e4"/>
            <SolidColorBrush x:Key="Danger" Color="#ef4444"/>

            <!-- Button Style -->
            <Style x:Key="HeaderButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="10,3"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2d2d2d"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PrimaryHeaderButtonStyle" TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#1A7B6CD9"/> <!-- Low opacity accent -->
                    </Trigger>
                </Style.Triggers>
            </Style>

                        <Style x:Key="MenuButtonStyle" TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="2">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- LEFT: Menu text -->
                                    <ContentPresenter Grid.Column="0"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>

                                    <!-- RIGHT: Chevron -->
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="12,0,12,0"
                                               Foreground="{StaticResource TextSecondary}"/>
                                </Grid>

                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#094771"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>

                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Window Control Button Style -->
            <Style x:Key="WindowControlButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="Width" Value="46"/>
                <Setter Property="Height" Value="35"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#333333"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButtonStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- TabItem Style -->
            <Style x:Key="EditorTabItemStyle" TargetType="TabItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0" Padding="{TemplateBinding Padding}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}" VerticalAlignment="Center" Margin="0,0,8,0"
                                    FontSize="12"/>
                                    <Button Name="CloseButton" Content="✕" FontSize="12" Background="Transparent" BorderThickness="0" Foreground="{TemplateBinding Foreground}" 
                                            Width="20" Height="20" Cursor="Hand" Tag="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#444444"/> <!-- Subtle Grey Hover -->
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource BgEditor}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextActive}"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="0,2,1,0"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ScrollBar Styles -->
            <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border x:Name="ThumbBorder" 
                                    Background="#3f3f46" 
                                    CornerRadius="4"
                                    Width="8"
                                    Margin="2,0"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#52525b"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#71717a"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border x:Name="ThumbBorder" 
                                    Background="#3f3f46" 
                                    CornerRadius="4"
                                    Height="8"
                                    Margin="0,2"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#52525b"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#71717a"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ScrollBar}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="12"/>
                <Setter Property="MinWidth" Value="12"/>
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="12"/>
                        <Setter Property="MinWidth" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid Background="Transparent">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollBarThumbVertical}"/>
                                            </Track.Thumb>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="12"/>
                        <Setter Property="MinHeight" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid Background="Transparent">
                                        <Track x:Name="PART_Track" IsDirectionReversed="False">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollBarThumbHorizontal}"/>
                                            </Track.Thumb>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False"/>
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Splitter Style -->
            <Style x:Key="EditorSplitterStyle" TargetType="GridSplitter">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GridSplitter">
                            <Border Background="Transparent" Padding="0">
                                <Rectangle Name="SplitterLine" 
                                           Fill="{DynamicResource SplitterBackground}" 
                                           Width=".5" 
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Stretch"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="SplitterLine" Property="Fill" Value="{StaticResource AccentColor}"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="SplitterLine" Property="Width" Value="12"/>
                                    <Setter TargetName="SplitterLine" Property="Fill" Value="{StaticResource AccentColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Light Theme Colors -->
            <SolidColorBrush x:Key="LightBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="LightForeground" Color="#000000"/>
            <SolidColorBrush x:Key="LightToolbarBackground" Color="#F0F0F0"/>
            <SolidColorBrush x:Key="LightEditorBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="LightEditorForeground" Color="#000000"/>
            <SolidColorBrush x:Key="LightLineNumbersForeground" Color="#2B91AF"/>
            <SolidColorBrush x:Key="LightSplitterBackground" Color="#DDDDDD"/>
            <SolidColorBrush x:Key="LightCurrentLineBackground" Color="#F5F5F5"/>
            
            <!-- Dark Theme Colors -->
            <SolidColorBrush x:Key="DarkBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="DarkForeground" Color="#D4D4D4"/>
            <SolidColorBrush x:Key="DarkToolbarBackground" Color="#2D2D30"/>
            <SolidColorBrush x:Key="DarkEditorBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="DarkEditorForeground" Color="#D4D4D4"/>
            <SolidColorBrush x:Key="DarkLineNumbersForeground" Color="#858585"/>
            <SolidColorBrush x:Key="DarkSplitterBackground" Color="#3E3E42"/>
            <SolidColorBrush x:Key="DarkCurrentLineBackground" Color="#282828"/>
            
            <!-- Default to Light Theme -->
            <SolidColorBrush x:Key="AppBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="AppForeground" Color="#000000"/>
            <SolidColorBrush x:Key="ToolbarBackground" Color="#F0F0F0"/>
            <SolidColorBrush x:Key="EditorBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="EditorForeground" Color="#000000"/>
            <SolidColorBrush x:Key="LineNumbersForeground" Color="#2B91AF"/>
            <SolidColorBrush x:Key="SplitterBackground" Color="#DDDDDD"/>
            <SolidColorBrush x:Key="CurrentLineBackground" Color="#282828"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
